name: 13 - Using caching

on:
  workflow_dispatch:
    inputs:
      use-cache:
        type: boolean
        default: true
        description: Whether to execute cache step
    
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working_dir: '13-caching/react-app'
    steps:
      - name: Checkout code
        run: |
          echo
      - name: Setup Node
        uses: actions/Setup_node@v3
          with:
            verion: 20.x
      - name: Install dependencies
        run: |
          npm ci
      - name: Testing
        run: |
          npm run Test 
      - name: Building
        run: |
          npm run build
      - name: Deploying to nonprod
        run: |
          echo "Deploying to nonprod"


